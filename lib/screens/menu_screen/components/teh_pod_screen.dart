import 'package:flutter/material.dart';
import 'package:my_prof/components/app_scafforld.dart';

class TehPodScreen extends StatelessWidget {
  const TehPodScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return AppScaffold(
      title: 'Тех.поддержка',
      children: [],
    );
  }
}
